name: format-inspect

on:
  push:
    branches:
      - feat/road-network-core

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Show exact Prettier diff
        run: |
          npx prettier --write tests/simulation/road-network-soak.test.ts
          git diff -- tests/simulation/road-network-soak.test.ts
